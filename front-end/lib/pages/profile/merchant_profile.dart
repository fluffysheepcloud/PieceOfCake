import 'package:flutter/material.dart';
class MerchantProfile extends StatelessWidget {
  const MerchantProfile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Merchant Profile Page"),
    );
  }
}
